'Imports iTextSharp.text.pdf
Imports System.Text


Public Class PdfToRtfConverter

	Public Shared Sub Convert()
		'Try
		'	Dim reader As New PdfReader("C:\\Documentación\\Mipdf.pdf")
		'	Dim dictionary As PdfDictionary = reader.GetPageN(1)
		'	Dim reference As PRIndirectReference = CType(dictionary.Get(PdfName.CONTENTS), PRIndirectReference)
		'	Dim stream As PRStream = CType(PdfReader.GetPdfObject(reference), PRStream)
		'	Dim bytes() As Byte = PdfReader.GetStreamBytes(stream)
		'	Dim tokenizer As New PRTokeniser(bytes)
		'	'Dim fos As New FileOutputStream("resume.rtf")
		'	Dim buffer As New StringBuilder()

		'	While (tokenizer.NextToken())
		'		If (tokenizer.TokenType() = PRTokeniser.TokType.STRING OrElse tokenizer.TokenType() = PRTokeniser.TokType.NUMBER OrElse tokenizer.TokenType() = PRTokeniser.TokType.NAME) Then
		'			buffer.Append(tokenizer.StringValue())
		'		End If
		'	End While

		'	Debug.Print(buffer.ToString)
		'Catch ex As Exception

		'End Try
		'try {
		'    Document document = new Document();
		'    document.open();
		'    PdfReader reader = new PdfReader("C:\\resume.pdf");
		'    PdfDictionary dictionary = reader.getPageN(1);
		'    PRIndirectReference reference = (PRIndirectReference) dictionary.get(PdfName.CONTENTS);
		'    PRStream stream = (PRStream) PdfReader.getPdfObject(reference);
		'    byte[] bytes = PdfReader.getStreamBytes(stream);
		'    PRTokeniser tokenizer = new PRTokeniser(bytes);
		'    FileOutputStream fos=new FileOutputStream("resume.rtf");
		'    StringBuffer buffer = new StringBuffer();
		'    while (tokenizer.nextToken()) {
		'    if (tokenizer.getTokenType() == PRTokeniser.TK_STRING) {
		'    buffer.append(tokenizer.getStringValue());
		'     }
		'       }
		'    String test=buffer.toString();
		'    StringReader stReader = new StringReader(test);
		'    int t;
		'	While ((t = stReader.read()) > 0)
		'    fos.write(t);
		'    document.close();
		'System.out.println("Converted Successfully");
		'}
		'     catch (Exception e) {}
		' }
	End Sub

End Class
